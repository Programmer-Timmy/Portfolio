meta {
  name: 2 CSRF token
  type: http
  seq: 2
}

get {
  url: {{apiUrl}}/auth/csrf
  body: none
  auth: none
}

headers {
  Cookie: {{logoutSession}}
}

script:post-response {
  if (res.body && res.body.data && res.body.data.token) {
    bru.setVar('logoutCsrf', res.body.data.token);
  }
}

assert {
  res.status: eq 200
}
