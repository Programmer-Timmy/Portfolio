meta {
  name: 3 Logout (204)
  type: http
  seq: 3
}

post {
  url: {{apiUrl}}/auth/logout
  body: none
  auth: none
}

headers {
  Cookie: {{logoutSession}}
  X-CSRF-Token: {{logoutCsrf}}
}

assert {
  res.status: eq 204
}
