meta {
  name: 07 Admin - Open source
  seq: 8
}

docs {
  Import a repo + its pull requests, then delete it. "02 Add repository" adds
  `octocat/Hello-World` (safe to remove); "06 Delete" cleans it up. Requires
  "00 Setup" and network access to api.github.com.
}
