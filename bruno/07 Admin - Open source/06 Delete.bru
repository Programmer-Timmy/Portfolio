meta {
  name: 06 Delete
  type: http
  seq: 6
}

delete {
  url: {{apiUrl}}/admin/opensource/{{osId}}
  body: none
  auth: none
}

headers {
  Cookie: {{sessionAdmin}}
  X-CSRF-Token: {{csrfAdmin}}
}

assert {
  res.status: eq 204
}

docs {
  Removes `octocat/Hello-World` and its imported PRs - leaves the tables clean.
}
