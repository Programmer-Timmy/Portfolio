meta {
  name: 19 Method not allowed (405)
  type: http
  seq: 19
}

put {
  url: {{apiUrl}}/health
  body: none
  auth: none
}

assert {
  res.status: eq 405
  res.body.error.code: eq method_not_allowed
}

docs {
  `/api/health` exists for GET only, so PUT returns 405 (not 404).
}
