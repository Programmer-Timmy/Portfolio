meta {
  name: 18 Unknown route (404)
  type: http
  seq: 18
}

get {
  url: {{apiUrl}}/does-not-exist
  body: none
  auth: none
}

assert {
  res.status: eq 404
  res.body.error.code: eq not_found
}
