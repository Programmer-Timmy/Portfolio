# Origin of the PHP backend during `npm run dev` (Vite proxies /api -> here).
# Start the PHP server with:  php -S localhost:8000 -t public
VITE_PHP_ORIGIN=http://localhost:8000

# Optional: override the API base path (default: /api, same origin).
# VITE_API_BASE=/api
